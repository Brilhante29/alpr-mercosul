name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Validate portfolio contract
        shell: pwsh
        run: ./tools/validate-project.ps1 -SkipDocker
      - name: Build image
        run: docker build --tag alpr-mercosul:${{ github.sha }} .
      - name: Run demo
        run: docker run --rm alpr-mercosul:${{ github.sha }} demo
      - name: Run benchmark
        run: |
          mkdir -p benchmarks/results
          docker run --rm \
            -v "$PWD/benchmarks/results:/app/benchmarks/results" \
            alpr-mercosul:${{ github.sha }} \
            benchmark --n-plates 100 --seed 42 --output /app/benchmarks/results/ci.json
      - uses: actions/upload-artifact@v7
        with:
          name: alpr-mercosul-benchmark
          path: benchmarks/results/ci.json
